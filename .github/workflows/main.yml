name: Release Note on Tag Push

on:
  push:
    tags:
      - '*'

jobs:
  create_release_note:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Get tag information
      run: |
        echo "##[set-output name=tag;]$(echo ${{github.ref}} | sed 's|refs/tags/||g')"
      id: get_tag_name

    - name: Generate release note
      run: |
        # 指定されたタグから1つ前のタグを検索
        export PREVIOUS_TAG=$(git describe --tags "${{ steps.get_tag_name.outputs.tag }}"^)
        # 指定された期間のコミットメッセージを取得
        export COMMIT_LOG=$(git log "$PREVIOUS_TAG"...HEAD --pretty=format:"%s")
        # リリースノートを生成
        export RELEASE_NOTES="## Changes since $PREVIOUS_TAG $COMMIT_LOG"

    - name: Create release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ steps.tag_info.outputs.tag }}
        release_name: Release ${{ steps.tag_info.outputs.tag }}
        body: |
          # Add the generated release notes here
          ${{ env.RELEASE_NOTES }}
